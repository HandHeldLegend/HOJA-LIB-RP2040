#ifndef HOJA_NESBUS_HAL_H
#define HOJA_NESBUS_HAL_H

// NESBUS (NES/SNES serial) transport HAL.
// Entry points (transport_nesbus_init / transport_nesbus_task) are declared in
// transport/transport_nesbus.h. Line params (PIO instance + data/clock/latch
// GPIO) are supplied by the board via hoja_config_s.nesbus.

#endif
