#ifndef HOJA_JOYBUS_N64_HAL_H
#define HOJA_JOYBUS_N64_HAL_H

// N64 joybus transport HAL.
// Entry points (transport_jb64_init / transport_jb64_task) are declared in
// transport/transport_joybus64.h. The PIO instance + data pin are supplied by
// the board via hoja_config_s.joybus, shared with the GameCube joybus HAL
// (only one is ever active at a time on the same physical line).

#endif
